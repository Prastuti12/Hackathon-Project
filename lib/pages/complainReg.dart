import 'package:flutter/material.dart';

class complainReg extends StatelessWidget {
  const complainReg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
       child: Column(
        
       ),
      ),
    );
  }
}
